import { PatientsManager } from "@/components/patients-manager";

/**
 * Patients Management page — allows adding, switching and viewing patient profiles.
 */
export default function PatientsPage() {
  return <PatientsManager />;
}
