import { MembersManager } from "@/components/members-manager";

/**
 * Members Management page — allows inviting and managing family members.
 */
export default function MembersPage() {
  return <MembersManager />;
}
